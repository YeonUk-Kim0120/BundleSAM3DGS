# VGGT 실패 원인 분리 지시서 (Probe 1b, 2026-09-26)

브랜치 `milestone5-v1` (`cadf3ee`). Probe 1(`EXP_VGGT_PROBE1_BRIEF.md`, `EXP_VGGT_PROBE1_RESULTS.md`)의 후속 진단 실험이다. 본 코드는 수정하지 않는다.
질문할 수 없으면 아래 규칙과 판독 기준대로 진행하고, 판단이 필요했던 지점은 결과 문서에 적는다.

---

## 0. 배경 (왜 하는가)

Probe 1 결과를 요약하면 다음과 같다.
- 넓은 기선의 기준 KF 배치(B-ref)는 새 KF 회전 14.6~34°로 무너졌다.
- 연속 6장 배치에서 겹침 1장을 GT로 맞추면 3.15°였다(양성 대조군 통과).
- 장치(변환·warp·채점)는 검증됐다.

그러나 **왜 무너졌는지는 분리되지 않았다.** 후보는 네 가지이고, 그중 둘은 지시서 설계 문제일 수 있다.

- **(가) 먼 기준 KF = 물체 반대편 뷰.** `select_reference`의 절반은 farthest-point로 뽑혀 사실상 반대편 뷰가 들어간다. 배경을 지운 물체 영상에서는 새 KF와 공유 표면이 없다.
- **(나) 비강건 정렬의 오염.** `align`은 모든 기준 KF 회전을 가중치 없이 평균한다(chordal mean). 먼 기준이 틀리면 새 KF가 가까운 기준과 맞아도 끌려간다.
  - "online ≈ oracle"은 이 가능성을 배제하지 못한다. oracle도 같은 평균을 쓰기 때문이다.
  - E2(모든 쌍 중앙값)도 먼 쌍이 다수면 커진다.
- **(다) 대칭 물체의 뒤집힘.** MPM12~14의 p90이 98~108°다. 약 180° 오차가 섞여 있다는 신호다.
- **(라) 큰 roll.** 손이 물체를 돌리므로 물체 기준 카메라가 광축 주위로 크게 기운 뷰가 섞인다. VGGT 학습 분포 밖일 수 있다. I1은 최소 회전만 해서 roll을 정규화하지 않았다.

VGGT 자체가 흔들린다는 신호도 있다. 넓은 배치에서 이동 스케일과 depth 스케일이 1.6~2.2배 어긋난다(E3).

**이번 실험의 목적은 두 가지다.**
1. 네 원인을 분리한다.
2. **핵심 질문에 답한다: 기선(시점 차)에 따라 VGGT 상대 포즈가 얼마나 정확하고, LoFTR+depth가 끊기는 기선에서 VGGT가 쓸 만한가.**
   - 가까운 시점끼리는 VGGT(연속 5장에서 2~3°)가 BSDF 국소 정밀도보다 약하다.
   - 따라서 VGGT가 가치를 가질 수 있는 곳은 LoFTR가 못 잇는 중간 기선(루프 제약 영역)뿐이다.

**하지 않는 것**: 본 코드 통합, depth 정밀화, 지도 학습, 모델 fine-tuning, M-Ω 추가 추론(A단계 재분석은 M-Ω 산출물도 포함).

---

## 1. 규칙

Probe 1 지시서 §1과 같다. 추가·변경 사항만 적는다.

**허용**
- `experiments/vggt_probe/` 아래 새 파일.
- `logs/exp_vggt_probe1b/`, `outputs/exp_vggt_probe1b/...` 새 디렉터리.
- 결과 문서 `EXP_VGGT_PROBE1B_RESULTS.md` 1개.

**금지**
- 기존 probe 파일(`probe_lib.py`, `run_probe.py`, `models.py` 등) 수정. import해서 재사용하고, 바꿀 부분은 새 파일에 구현한다. Probe 1 결과의 재현성을 지키기 위해서다.
- Probe 1 산출물(`outputs/exp_vggt_probe1/`) 수정·삭제. 읽기 전용이다.
- 커밋. 사용자가 승인하면 그때 한다.

**운영**
- `logs/exp_vggt_probe1b/PROGRESS.md`를 단계마다 갱신한다.
- 실행마다 매니페스트를 남긴다.
- 실패하면 1회 재시도 후 건너뛴다.
- 디스크 150 GB 규칙, 컨테이너·venv는 Probe 1과 같다.
- 모든 하이퍼파라미터는 B단계 시작 전에 고정한다. 시퀀스별 튜닝은 금지다.

---

## 2. 정의 (모든 분석 공통)

**프레임 포즈 복원 (Probe 1과 동일)**
- `batches.jsonl`의 `w2c_raw`, `R_virtual`, `s`로 `c2w_b,i = inv(w2c_raw_i)`을 만든다.
- 이동 성분에 s를 곱한 뒤 `· blockdiag(R_virtual_i, 1)`을 적용한다.

**쌍 (i, j)의 상대 포즈**
- `rel = inv(c2w_i) @ c2w_j`로 정의한다. i의 카메라 좌표계에서 본 j다.
- 상대 회전 오차: `rot_deg(rel_est[:3,:3], rel_gt[:3,:3])`.
- 상대 이동 방향 오차: 두 이동 벡터의 각도(°).
- 이 두 값은 게이지와 스케일에 무관하다.

**기선 분해 (GT 기준, swing–twist)**
- `R_rel_gt = R_i,gtᵀ R_j,gt`로 둔다.
- **swing** = `R_rel_gt·ẑ`와 `ẑ` 사이 각. 광축 방향 차이이며, Probe 1의 광축 각과 같은 양이다.
- **twist(roll)** = `R_rel_gt`를 ẑ축에 대해 swing–twist 분해한 twist 각의 절댓값이다.
  - 쿼터니언 `(w,x,y,z)`에서 `2·atan2(|z|, w)`로 계산하고, [0°,180°]로 접는다.
- **bin**
  - swing: [0,15), [15,30), [30,60), [60,90), [90,135), [135,180]
  - roll: [0,15), [15,45), [45,90), [90,180]

**쌍 종류 (B-ref 배치)**
- `ref` 리스트는 선택 순서대로 [첫 자리, 가까운 것 n_near장, farthest-point 나머지]다. n_near = (k−1)//2이고, 후보가 k보다 적으면 있는 만큼이다.
- 역할: `first`, `near`, `far`, `new`.
- 쌍 종류: `new–first`, `new–near`, `new–far`, `new–new`, `ref–ref`.

**비교 기준 쌍 오차**
- 같은 쌍에 대해 BSDF 포즈로 만든 상대 포즈의 오차도 계산한다.
- **C2 쌍 오차**가 주 기준이다. 최종 스냅샷 하나에서 나온 일관된 포즈이기 때문이다.
- **C1 쌍 오차**는 참고용이다. 두 KF의 소비 시점이 달라 시각이 섞인다.

---

## 3. A단계 — 기존 출력 재분석 (GPU 불필요)

**대상**: `outputs/exp_vggt_probe1/` 전 셀(HO3D·YCB, M-V·M-Ω, I0·I1·I2, 체인·B-ref). 추론은 다시 하지 않는다.

**A0. 재현 확인 (필수, 통과 전 다른 결과를 내지 않음)**
- 무작위 3개 셀에서 `batches.jsonl`만으로 새 KF의 online/oracle c2w를 재구성한다.
- `kf.csv`의 `c2w_online_*`, `c2w_oracle_*`와 비교해 1e-6 안에서 같아야 한다.
- 결과를 PROGRESS에 적는다.

**A1. 기선별 정확도 곡선 (핵심)**
- 모든 배치의 모든 쌍(두 프레임 모두 GT 있음)에 대해 한 행씩 `pairs.csv`로 저장한다.
- 열:
  - 셀 정보: 모델, 입력, 배치 방식, seq, 배치 id, i/j kf id, 역할, 쌍 종류
  - 기선: swing, roll
  - VGGT 오차: 상대 회전 오차, 이동 방향 오차
  - 비교 오차: C2 쌍 회전 오차, C1 쌍 회전 오차
  - 두 프레임의 마스크 면적, VGGT conf 평균
- 표 P1: 모델/입력별로 swing bin마다 다음을 적는다.
  - VGGT 쌍 회전 오차의 중앙값, p75, p90, >30° 비율, >90° 비율, 개수
  - 같은 쌍의 C2 쌍 오차 중앙값, p90
  - 체인 셀과 B-ref 셀은 따로 적는다.
- 표 P2: M-V/I1 B-ref에 대해 swing × roll 2차원 표(중앙값, >30° 비율, 개수).
- 표 P3: M-V/I1 B-ref에 대해 쌍 종류별 swing bin 표. 먼 기준이 낀 쌍과 아닌 쌍을 가른다.

**A2. 정렬 재계산 (원인 (나) 분리)**

M-V/I1과 M-Ω/I1의 B-ref 셀 전부에 대해, 저장된 VGGT 출력으로 새 KF 포즈를 다시 정렬한다. 정렬 방식은 다섯 가지다.
- **(a) 기존:** 전 기준 chordal mean + 이동 median. 저장값과 일치해야 한다.
- **(b) 첫 자리만:** 첫 자리 기준 1장.
- **(c) 먼 기준 제외:** 첫 자리와 near만.
- **(d) 강건:**
  1. 각 기준 r 한 장으로 T_r을 만든다.
  2. 나머지 기준들의 회전 잔차 중앙값이 가장 작은 r을 고른다.
  3. 그 T_r에서 회전 잔차가 10° 미만인 기준들만으로 (a) 방식 재정렬을 한다. 이 기준이 1장뿐이면 T_r을 그대로 쓴다.
- **(e) oracle 상한 진단:** 새 KF를 첫 자리 기준과의 VGGT 상대 포즈만으로 놓는다(첫 자리는 GT). 이는 (b)의 oracle과 같으니 표에서 (b)-oracle로 대신해도 된다.

(a)~(d) 각각에 대해 online(R-online)과 oracle(GT) 기준을 모두 계산한다. 표 R에 적을 것:
- HO3D 시퀀스별 새 KF 회전 중앙값과 p90, 이동 중앙값
- 시퀀스 평균
- C1보다 나은 시퀀스 수
- 비교 행: C1, C2, 체인 (2) R-online, 체인 (2) R-oracle

**A3. 뒤집힘 분석 (원인 (다))**
- 새 KF E1 오차와 쌍 오차에서 >90°, >150° 비율을 시퀀스별로 구한다.
- >90°인 경우 오차 회전(`R_est R_gtᵀ`, 물체 좌표계)의 회전축을 구한다.
- GT 물체 메시 꼭짓점의 PCA 주축 3개와의 최소 각을 계산한다. GT 메시 경로는 `eval_mesh_cd.py`가 쓰는 것을 따른다.
- 오차 각도 히스토그램(10° 간격)을 만들고, 뒤집힘 쌍의 쌍 종류 구성도 적는다.
- 표 FL.

**A4. 상관**
- M-V/I1 B-ref 쌍 오차와 다음 변수 사이의 Spearman ρ를 구한다.
  - swing, roll
  - min(마스크 면적)
  - 마스크 면적 비(작은 것/큰 것)
  - conf 평균
  - 배치 T3
- 구한 뒤 swing bin 안에서 다시 구한 것도 적는다(swing 효과 통제).

---

## 4. B단계 — 소규모 추론 (M-V만)

**시퀀스 (5개)**
- AP12: 보통 경우
- MPM10: 최대 오차
- MPM12: 뒤집힘 의심
- SB11: 큰 오차
- SM1: 긴 시퀀스

**기본 설정**
- M-V, I1, M=1, k=8.
- 정렬은 (a) 기존과 (d) 강건을 모두 계산한다.
- R-online과 R-oracle 둘 다 계산한다.
- 모든 배치의 쌍을 A1과 같은 형식의 `pairs.csv`로 저장한다.

**B1. 가까운 기준만 (원인 (가))**
- 기준 선택 규칙을 "새 KF와 광축 각이 작은 순으로 k장"으로 바꾼다. farthest-point는 없다.
- k=8과 k=4 두 가지를 돌린다.
- 선택에 쓰는 포즈는 Probe 1과 같다: 기준은 R-online, 새 KF는 C1.

**B2. 기선 대역 배치 (핵심 질문, 맥락 있는 중간 기선)**
- 첫 자리: 광축 각이 가장 작은 기준.
- 나머지 기준은 광축 각 대역마다 2장씩 고른다: [15,30), [30,60), [60,90). 대역 중앙각에 가까운 순으로 고른다.
- 비는 대역은 가장 가까운 순으로 채우고, 실제 구성을 기록한다.
- 총 기준 k=7, 새 KF 1장.
- 목적: 반대편 뷰 없이 중간 기선 쌍을 만들어 대역별 정확도를 잰다.

**B3. roll 정규화 가상 카메라 (원인 (라))**
- I1의 중심 회전 `R_c,i` 뒤에 광축(z) 주위 회전 `R_z(ψ_i)`를 추가한다. 가상 회전은 `R_v,i = R_z(ψ_i)·R_c,i`가 된다.
- **ψ_i 결정**
  1. 배치 첫 자리 기준의 카메라 위쪽 방향을 물체 좌표계로 옮긴다: `u = −R_first[:,1]` (R-online 포즈).
  2. 프레임 i의 카메라 좌표로 옮긴다: `u_ci = R_iᵀ u`. R_i는 기준이면 R-online, 새 KF면 C1이다. 온라인에서 알 수 있는 정보만 쓴다.
  3. 가상 좌표로 옮긴다: `u_vi = R_c,i u_ci`.
  4. `(u_vi.x, u_vi.y)`가 이미지 위쪽(−y)을 향하도록 ψ_i를 정한다.
  5. 투영 길이가 0.2 미만이면(u가 거의 광축 방향) ψ_i = 0으로 두고 기록한다.
- warp와 포즈 복원은 `R_c,i` 대신 `R_v,i`를 쓰는 것 말고는 I1과 같다.
- **단위 확인 (필수)**: Probe 1 0-a·0-b와 같은 합성 왕복(오차 < 1e-6)과 warp IoU(> 0.98)를 roll 포함으로 다시 통과시킨다.
- B1(k=8) 배치 구성에 적용한다(B1-roll).

**B4. 배경색**
- B1(k=8) 구성에서 마스크 밖을 검정(0.0)과 회색(0.5)으로 채운 두 조건을 추가한다. 흰색은 B1 자체다.

**B5. 음성 대조군 (마스크 없음)**
- AP12, SM1에서 체인 배치로 원본 이미지를 마스크 없이 I0 방식(패딩만)으로 넣는다.
- VGGT 상대 회전 크기 중앙값을 GT 상대 회전 크기 중앙값과 나란히 적는다.
- 예상: 배경이 고정이라 VGGT는 거의 0을 낸다. 마스킹의 필요성을 확인하는 용도다.

**B6. 쌍 벤치마크: LoFTR+depth vs VGGT (핵심 질문)**
- **쌍 표본**: 5개 시퀀스의 키프레임 쌍을 GT swing bin(2절)별로 최대 60쌍씩 무작위 추출한다. 시드는 고정한다.
- **추정기 3종** (같은 쌍에 적용)
  1. **LoFTR+depth**
     - `loftr_wrapper.LoftrRunner`를 쓴다. 전처리(물체 영역 크롭·리사이즈·회전 처리 등)는 `bundlesdf.py`와 BundleTrack C++ `findCorres` 계열이 LoFTR를 호출하는 방식을 읽고 그대로 따른다.
     - 대응점을 두 마스크 안, depth 유효 픽셀로 거른다.
     - 3D-3D RANSAC + Procrustes를 한다. 임계값은 `BundleTrack/config_ho3d.yml`의 해당 값을 쓴다.
     - 기록: 인라이어 수, 성공 여부, 성공 시 상대 회전 오차.
     - 성공 = 인라이어 ≥ config의 최소 대응 수(없으면 30).
  2. **VGGT 2장**: [i, j] 두 장만 I1로 넣은 상대 포즈(VGGT-SLAM 2의 루프 서브맵과 같은 형태).
  3. **VGGT 2장 + roll 정규화**: B3 방식. u는 i의 R-online 포즈에서 만든다.
- 표 L: swing bin마다 다음을 적는다.
  - LoFTR 성공률, 성공 쌍의 회전 오차 중앙값과 p90
  - VGGT 2장 오차 중앙값, p90, >30° 비율
  - VGGT 2장+roll 오차(같은 통계)
  - 같은 쌍의 C2 쌍 오차 중앙값
  - 시퀀스별 표와 5개 합계 표를 모두 만든다.

**B 결과 표**
- 표 B: 조건(B1-k8, B1-k4, B2, B1-roll, B4-검정, B4-회색, 비교용 Probe 1 ref1-8)마다 시퀀스별로 다음을 적는다.
  - 새 KF E1 회전 중앙값과 p90: online/oracle × 정렬 (a)/(d)
  - 쌍 종류별 쌍 오차 중앙값
  - E3 중앙값
- 표 P-B2: B2의 대역별 쌍 오차(new–대역 기준). 중앙값, p90, >30% 비율.
- 표 NC: B5 음성 대조군.

---

## 5. 순서·예산

1. **A0 → A1 → A2 → A3 → A4** (CPU). 코딩을 포함해 2~3시간.
2. **B3 단위 확인** → **B1 → B2 → B1-roll → B4 → B5** (GPU 2장 병렬). 조건×시퀀스당 3~5분, 합계 약 1.5~2시간.
3. **B6**. 1,800쌍 이하, LoFTR 쌍당 약 25 ms, VGGT 2장 약 0.3 s × 2. 로딩 포함 1시간 이내.
4. 표 작성과 결과 문서.

**시간이 부족할 때**
- 생략 순서: A4 → B4 → B5 → B1-k4.
- 반드시 끝낼 것: A0, A1, A2, A3, B1-k8, B2, B1-roll, B6.

---

## 6. 산출물

- `outputs/exp_vggt_probe1b/A/`: 셀별 `pairs.csv`, `realign.csv`(새 KF × 정렬 방식 × online/oracle).
- `outputs/exp_vggt_probe1b/B/<cond>/<seq>/`: Probe 1과 같은 `kf.csv`, `batches.jsonl`, `pairs.csv`, `manifest.json`.
- `outputs/exp_vggt_probe1b/B6/`: `pairs_bench.csv`. 열은 쌍, swing, roll, LoFTR 인라이어·성공·오차, VGGT 2장 오차, VGGT 2장+roll 오차, C2 쌍 오차.
- `logs/exp_vggt_probe1b/`: PROGRESS.md, manifests, 표 md.

**결과 문서 `EXP_VGGT_PROBE1B_RESULTS.md`**
1. 한 페이지 요약: 원인 (가)~(라) 각각의 판독 결과와 수치, 핵심 질문(표 L)의 답.
2. 표 P1, P2, P3 (A1)
3. 표 R (A2)
4. 표 FL (A3), A4 상관
5. 표 B, 표 P-B2, 표 NC
6. 표 L (B6)
7. 계획 이탈, 승인 필요 사항

**판독 기준 (잠정; 판정은 사용자가 한다, 표는 정확히)**
- **(나) 정렬 오염이 주원인**: A2에서 (d) 또는 (c)가 (a)보다 새 KF online 회전 중앙값을 30% 이상 줄인다.
- **(가) 먼 기준이 주원인**: B1-k8의 oracle 중앙값이 Probe 1 체인 (2) R-oracle(M-V/I1, 같은 5개 시퀀스 평균) + 1° 이내.
- **(라) roll이 원인**: B1-roll이 B1-k8 대비, roll > 45° 쌍의 오차 중앙값을 20% 이상 줄인다.
- **(다) 뒤집힘**: 시퀀스별 >90° 비율, 그리고 그중 far 쌍이 차지하는 몫을 적는다. 판정은 사용자가 한다.
- **배경색**: B4 조건 간 새 KF 중앙값 차이가 1° 미만이면 "영향 없음"으로 적는다.
- **핵심 질문 — VGGT가 중간 기선에서 쓸 만한가**
  - LoFTR 성공률이 50% 미만인 swing bin이 있고, 그 bin에서 VGGT(2장 또는 B2 대역 배치) 중앙값 ≤ 5°이면서 p90 ≤ 15°이면 "루프 제약 후보로 유효"라고 적는다.
  - 그런 bin이 없으면 "LoFTR가 끊기는 기선에서 VGGT도 쓸 수 없다"고 적는다.
  - 두 경우 모두 VGGT를 쓸 수 있는 swing 상한(중앙값 5° 이하인 마지막 bin)을 명시한다.

---

## 7. 주의

- swing/roll 분해와 쌍 오차는 GT 기준 분석용이다. B단계의 입력 구성(기준 선택, roll 정규화)에는 GT를 쓰지 않는다. 기준은 R-online, 새 KF는 C1 포즈만 쓴다.
- A2 (a)가 저장값과 일치하지 않으면 A2 전체를 중단하고 원인을 PROGRESS에 적는다.
- 쌍 통계는 긴 시퀀스가 지배하지 않도록 합계 표와 함께 시퀀스별 표를 반드시 둔다.
- 뒤집힘(>90°)이 섞인 분포는 평균이 무의미하다. 중앙값, p90, >30%·>90% 비율로 보고한다.
- LoFTR 전처리를 BundleTrack과 다르게 하면 LoFTR 성공률이 과소평가된다. 실제로 따라 한 전처리를 결과 문서에 구체적으로 적는다(크롭 방식, 해상도, 회전 처리 여부, 임계값 출처).
