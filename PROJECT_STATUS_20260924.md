# BundleSAM3DGS 총정리 (2026-09-24)

지금까지의 마일스톤, 실험 흐름, 각 단계에서 얻은 결론, 현재 상태에 대한 총평. 수치는 모두 같은 채점기로 22시퀀스(HO3D 13 + YCBInEOAT 9, SAM2 마스크)에서 계산한 값이며, 근거 문서는 §7에 있다. 기준 코드 `milestone5-v1` `5c28f99` (+ 미커밋: 2차 배치 결과·스크립트).

---

## 1. 무엇을 만들려고 했나

원본 BundleSDF(CVPR 2023)는 RGB-D 영상에서 미지 물체를 추적(BundleTrack: LoFTR 대응 → RANSAC → 키프레임 포즈그래프 BA)하면서 별도 프로세스의 신경 SDF로 물체를 복원하고, SDF가 정제한 키프레임 포즈를 트래커에 되돌려 써넣는다. 한계: 카메라가 못 본 면은 구멍으로 남고, SDF 학습이 무겁다.

우리 목표는 두 가지였다.
1. **완성 메시**: SDF를 2DGS 지도로 바꾸고, SAM3D 첫 프레임 전체 형상 프라이어를 넣어 한 번도 못 본 면까지 메시를 완성한다(원본과의 차별점).
2. **양방향 루프(⑤)**: GS 지도가 트래커 포즈를 되돌려 고친다. 성공 기준 = 트래커 단독보다 좋아야 한다.

전략은 "파이프라인을 끝까지 연결한 뒤 성능을 올린다"였다.

## 2. 진행 흐름 (시간순)

| 시기 | 단계 | 한 일 | 결과·결정 |
|---|---|---|---|
| ~8/31 | ① 2DGS 표현 | 2DGS 렌더러 + 깊이 Huber + 법선 일관성 손실 | 3DGS보다 형상 우수 → 채택 |
| 8/31 | ② SAM3D 프라이어 + 정합 | SAM2 마스크로 SAM3D 추론, 메시→서펠, 첫 프레임 렌더-비교 Sim(3) 정합, 색 전이 | 초기 4.4~46 mm → 2.7~8 mm, 22시퀀스 평균 3.13 mm → 채택(arm C) |
| 9/1 | ③ 생애주기 | UNSEEN/VERIFIED/SUSPECT/CONTRADICTED 4상태, 가림 보호, 스침각 지지 판정; UNSEEN 동결 | 못 본 영역 GT 거리는 동결이 최선(4.9 mm) → 채택, 편향장(③b) 기각 |
| 9/1 | ④ 온라인 결합 | `run_gaussian` 프로세스, 5 키프레임 대기, 키프레임마다 추가·분류·학습·써넣기 | mustard0 완주, ADD 0.758 = 트래커 단독 |
| 9/1 | ⑤ 1차 | 델타 클램프 방식 피드백, 교란-복구 실험 | 회전 24 %만 회복, 이동 실패 → 롤백, `se3_exp` 버그 발견 |
| 9/3~4 | SDF 피드백 공정 평가 | 같은 SDF 백엔드에서 on/noop/off 22시퀀스 66회, 원본 출력과 대조 | 우리 저장소 = 원본 검증. HO3D: SDF on 0.728 / off 1.674(붕괴 SM1 4.36→0.55). YCB: on 1.489 / off 1.275(트래커 단독이 최선). ⑤ 설계 조건 = "무너질 때만 강하게" |
| 9/7~11 | ⑤ 2차 | 원본 PoseArray 구조 이식(v1), 사전정렬, 프라이어 불투명도, 키프레임 할당, 손실 정책들, GT-지도 프로브 | YCB v1 1.214(원본 이김), HO3D v1 2.407(단독 1.674보다 나쁨). **GT 포즈로 만든 지도면 기울기 정답률 99 %, 트래커 지도 40 %** → 병목은 손실이 아니라 트래커 오차를 흡수한 지도. ⑤ 보류, 본 코드 = 안전지대 + v1 |
| 9/11~12 | 전역 정제 + 메시 | 24개 설정 비교, Poisson 주 메시, P1/P2/P3 채점기, 통합 | 22시퀀스: HO3D P1 0.459(원본 0.478), 커버리지 77 %(원본 53 %) |
| 9/12~17 | B-트랙 | 지도 결함 진단(반투명층, 스케일 폭주, 마스크 누출), 4시퀀스 설정 26개, 22시퀀스 확인 2회 | 초기화 500 + 침식 2 px 채택(HO3D ADD 2.064→1.867, P1 0.405); 융합 F·lr·깊이 가중치·정합 프레임 수 기각; 속도 옵션 기록 |
| 9/16~17 | ⑤ 원인 분석 | 프레임별 ADD 곡선, 써넣기 2,949건 분석, 게이트 검토 | 써넣기 득실 50/50, 움직임·오차 급등·delta·인라이어와 무관 → 게이트 철회 |
| 9/18 | 1차 결론 + 속도 | 통합 표(원본 표기법), 논문 조건 속도 측정, 6DOPE-GS 비교 | 논문 10 Hz는 추적 스레드 구성 요소 합; 공개 코드는 원본·우리 모두 5 Hz 상한, 전체 1~3 Hz |
| 9/21~22 | 무인 배치 1차 (H1~H5) | 법선 초기화, 학습 스텝, 이중층, 스프링, 포즈 lr, BundleSDF 포즈 재생 | H5(포즈 최적화 잡음) 지지; 좋은 포즈면 메시 P1 0.320(원본 0.478); H1 사실이나 병목 아님, H3·H4 기각 |
| 9/22~23 | 무인 배치 2차 (Q1, Q2) | prior 4단 ablation, 온라인 조건 하이브리드 | prior 기여 확인, lifecycle 화해 효과 거의 없음; 하이브리드는 층 재앵커링 시 상한 도달 |

## 3. 각 단계에서 알게 된 것

### 3.1 복원 (지도·메시)
- 우리 메시는 22시퀀스 모두에서 원본보다 좋다: HO3D P1 0.405 vs 0.478, P2 0.343 vs 0.490, 커버리지 78 vs 53 %; YCB P1 0.453 vs 0.723, 커버리지 59 vs 32 %. seen 영역 정확도도 우리가 좋다(HO3D 0.250 vs 0.333, 19/22).
- **포즈만 좋으면 훨씬 더 좋아진다**: 원본의 최종 포즈로 우리 지도를 만들면 HO3D P1 0.320, 붕괴 시퀀스 SM1 0.213(원본 메시 0.421), AP10 0.264(0.494). HO3D 복원의 병목은 포즈다(1차 배치 4.6).
- **SAM3D prior는 필수**: 없으면 P1이 22/22 악화(HO3D 0.446, YCB 1.351), 커버리지 HO3D 85→44 %. 보인 면 정확도도 21/22 악화(2차 Q1).
- **lifecycle의 화해 효과는 현재 지표로 거의 없다**: prior를 꽂기만 한 frozen이 full과 HO3D 7/13, YCB 8/9 잡음 범위이고, prior가 틀린 6개에서는 full이 이기는 곳이 없다(cracker는 frozen이 0.05 우세). initonly도 같다(2차 Q1). ③의 가치는 "프라이어 보존"이지 "충돌 화해"가 아니다.
- 학습(500/500)의 메시 기여는 작고 시퀀스에 따라 부호가 바뀐다: 트래커 포즈에서 학습 0이 AP12에 유리(0.353 vs 0.418), MPM12에 불리; 좋은 포즈에서는 평균적으로 필요(0.322→0.378, 8 악화/3 개선).
- 지도 위생: 관측 가우시안의 67~73 %가 거의 투명(불투명도 < 0.1), 재관측의 92~94 %가 기존 표면 위 2~5 mm에 중복 생성, 렌더 깊이 오차 3.7 mm(서펠 중심은 GT에서 2~4 mm). 관측 disk는 전부 첫 카메라 광축을 향한 채 태어나며 45° 넘는 오차는 학습이 못 고친다(H1 확정) — 그러나 고쳐도 메시는 그대로. 반투명층·이중층(H3)도 추출 단계에서 메시를 해치지 않는다. 면내 반지름 5 mm clamp는 큰 가우시안을 0으로 만들고 렌더 잔차를 27 % 줄이며 메시는 그대로(채택 후보).
- 메시 추출: 서펠 중심 + 법선 Poisson이 TSDF(학습 뷰·가상 뷰)와 SuGaR식 렌더 기반보다 낫다. 밀도 컷은 0이어야 prior가 채운 뒷면이 남는다.

### 3.2 추적 (⑤ 피드백)
- HO3D: 원본 SDF 피드백 0.728 ≪ 트래커 단독 1.674 < 우리 GS 피드백 1.867. 우리 피드백은 트래커 단독보다 나쁘다. YCB: 우리 1.199 < 트래커 단독 1.275 < 원본 1.503(원본 SDF는 대칭 물체에서 해악).
- 원인 규명의 결론들: (a) 지도가 트래커 오차를 흡수한다(GT 지도 99 % vs 40 %); (b) 써넣기 한 건의 효과는 ±0.1 cm 잡음이며 키프레임 시점의 어떤 관측량과도 무관(2,949건); (c) 포즈 최적화 자체가 잡음을 만든다 — lr 0.01→0.001에서 이동량 3~4배 감소, 멀쩡한 시퀀스 해악 1/4(AP12 +1.22→+0.32, MPM11 +0.49→+0.11) 대신 붕괴 구조 절반(SM1 −1.29→−0.61); (d) 자기 층 스프링(H4)은 주된 원인이 아님; (e) 원본이 되는 이유는 사이클마다 SDF와 포즈를 새로 맞추고 전 키프레임을 공동 정제하는 큰 보정이며, 뷰 하나씩 500스텝 델타로는 붕괴를 못 잡는다.
- 첫 써넣기(초기화 5장)는 프라이어 정합 오차와 같이 커지고 HO3D에서 즉시 해친다(AP12 +0.53).
- 6DOPE-GS(같은 계열, prior 없음)도 HO3D ADD AUC 84.3으로 원본(89.6)에 못 미친다. 그들의 차이점: 나쁜 키프레임 필터(MAD), 지도 고정 후 포즈만 2단계 최적화, 커버리지 기반 희소 키프레임, 퍼센타일 프루닝, 비동기.

### 3.3 하이브리드 (포즈 원본 NOF, 복원 GS) — 2차 Q2
- 온라인 조건(키프레임을 소비 시점 포즈로 쌓고 NOF가 뒤에서 포즈를 고침; 평균 1.7°/14 mm, AP11 5°/60 mm)에서: 통합만(consume) 0.334, 뷰 포즈만 갱신(현재 본 코드) 0.328, **층까지 재앵커 0.319 = 상한 0.320(13/13 잡음 범위)**. MPM10·SB11은 재앵커링이 있어야 닫힌다.
- 열린 문제: 재앵커 지도는 Gaussian 수가 30~80 % 많다(메시 영향 없음). 원인 미확정.

### 3.4 속도
- 논문 10.2 Hz = 추적 스레드 구성 요소 합(분할 18 + 매칭 24 + 포즈 그래프 56 ms). 공개 코드는 매 프레임 키프레임 8~9쌍 LoFTR(17~25 ms/쌍)라 원본·우리 모두 추적 스레드 5 Hz 상한. 기본값 엄격 동기라 전체 1~2 Hz, 비동기면 2.5~3.4 Hz(원본과 동급). GS 백엔드 라운드 5~10 s(지도 크기 비례), SDF 6~7 s.

## 4. 현재 상태 총평

**되는 것**
1. 완성 복원. 프라이어 덕에 못 본 면이 채워지고(커버리지 78/59 % vs 원본 53/32 %), 보인 면도 원본보다 정확하다. 이것은 22시퀀스에서 재현되고, 포즈를 통제한 실험에서 prior의 기여가 분리 확인됐다. 논문의 첫 번째 주장은 근거가 있다.
2. 포즈가 좋을 때의 복원 상한이 높다(HO3D P1 0.320). 원본 포즈 + 우리 복원은 온라인 조건에서도 그 상한에 도달한다(층 재앵커링 필요).
3. YCB 추적은 원본과 트래커 단독을 이긴다.

**안 되는 것**
1. ⑤ 양방향 루프. HO3D에서 우리 피드백은 트래커 단독보다 나쁘고 원본과 격차가 크다. 두 번의 설계 시도, 손실 정책 6종, 게이트, lr 조정, 스프링/앵커 실험을 거쳐 "해악은 최적화 잡음 + 트래커 오차를 흡수한 지도" 까지는 규명됐지만, 트래커를 이기는 구성은 아직 없다. lr 0.001은 해악을 줄일 뿐 이득을 만들지 못한다.
2. lifecycle(③)의 "화해" 주장. 프라이어를 꽂기만 한 것과 지표 차이가 없다. ③은 "프라이어 보존 + 충돌 제거"로 축소해서 말해야 한다.
3. 지도 위생(반투명층, 중복, 방향)은 사실이지만 메시 지표를 움직이지 않는다. 이쪽 실험은 더 할 이유가 없다.

**정확한 위치**
- 논문의 두 주장 중 "완성 메시"는 성립하고 "양방향 루프"는 성립하지 않는다.
- 시스템으로서는 세 갈래가 남는다. (a) GS 피드백을 계속 고친다 — 남은 후보는 여러 뷰 공동 최적화(잡음 평균화)와 붕괴 시 큰 보정, 6DOPE-GS식 키프레임 필터·2단계 포즈 최적화. 지금까지의 근거로는 성공을 장담할 수 없다. (b) 하이브리드: 포즈는 원본 SDF 피드백, 복원은 GS. 온라인 조건에서 상한에 도달함이 확인됐고 원본 대비 복원 이득이 확실하다. 단 SDF와 GS 두 백엔드를 동시에 돌리는 비용과 "GS만으로"라는 주장의 포기가 따른다. (c) 피드백 없이 트래커 단독 + GS 복원: HO3D ADD 1.674, 복원은 우리 포즈 기준(P1 0.459). 가장 단순하지만 HO3D 추적이 원본보다 2.3배 나쁘다.

## 5. 남아 있는 결정과 미해결 항목
1. 시스템 방향 (a)/(b)/(c).
2. 본 코드 반영 후보: scaleclamp(안전), 포즈 lr 0.001(선택), 층 재앵커링(하이브리드 채택 시; 중복 문제 해결 후), 법선 초기화(옳지만 효과 없음).
3. 미해결: 재앵커 시 Gaussian 30~80 % 증가; 프라이어 스케일 오차(YCB 4개 16~33 %, 결과와 상관 0.65~0.83)와 정합 best 선택 오류(깊이 항 정규화·마지막 포즈 사용 제안); YCB P3 라벨의 손 가림 미모델링.
4. 정리 필요: 실험 복사본 러너의 검증된 변경(침식·법선 초기화·clamp)만 본 코드로, 복사본 축소; 2차 배치 결과 커밋; `origin` 푸시.

## 6. 잡음 바닥 (판정 기준)
ADD HO3D ±0.2 cm(불안정 시퀀스 AP10·MPM10은 단일 실행 판정 불가: AP10 noop 3회 2.6/3.8/4.6), YCB ±0.05; P1/P2 ±0.03; 커버리지 ±5 pt(MPM12 반복 ±8).

## 7. 문서·산출물
- 통합 표: `RESULTS_22SEQ.md`(생성 `experiments/build_results_tables.py`). 1차 결론: `FIRST_CONCLUSION_20260918.md`(평가 정의, 메시 추출 4종, 속도).
- 마일스톤 기록: `MILESTONES.md`, `SAM3D_ALIGNMENT_RESULTS.md`, `PRIOR_LIFECYCLE_RESULTS.md`, `ONLINE_GS_BACKEND_RESULTS.md`, `MILESTONE5_FEEDBACK_RESULTS.md`, `MILESTONE5_CHANGE_LEDGER.md`, `GLOBAL_REFINE_RESULTS.md`, `ONLINE_MAP_DEFECTS.md`(B-트랙 §0~15).
- 무인 배치: `EXP_BATCH_20260921_{BRIEF,RESULTS}.md`, `EXP_BATCH_20260922_{BRIEF,RESULTS}.md`, `logs/exp_batch_2026092{1,2}/`.
- 출력(덤프 정리 후): `outputs/fulleval_20260912` 22 GB, `outputs/exp_fusion_20260915` 50 GB, `outputs/safety22_20260917` 14 GB, `outputs/exp_batch_20260921` 46 GB, `outputs/exp_batch_20260922` 83 GB, `outputs/runtime_20260918` 3 GB.
- 커밋: 안전지대 `c297f6a` → 분석 `86c2766` → 1차 결론 `9f2ea65` → 1차 배치 `5c28f99` → 2차 배치 `8d027e9`(사용자가 다른 세션에서 커밋·푸시, `origin/milestone5-v1`과 일치). 이 문서만 미커밋.
