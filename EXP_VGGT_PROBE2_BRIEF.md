# VGGT 모델 렌더 앵커 정합 지시서 (Probe 2, 2026-09-26)

브랜치 `milestone5-v1` (`bbce546`). Probe 1·1b(`EXP_VGGT_PROBE1*_RESULTS.md`)의 후속이다. 본 코드는 수정하지 않는 오프라인 실험이다.
질문할 수 없으면 아래 규칙과 판독 기준대로 진행하고, 판단이 필요했던 지점은 결과 문서에 적는다.

---

## 0. 배경 (왜 하는가)

**Probe 1·1b에서 확인한 것**
- VGGT(마스크한 물체 영상)는 가까운 뷰에서만 정확하다. 시점 차(swing) 15° 이내, roll 45° 이내에서 상대 회전 2~3°다.
- 시점 차가 30°를 넘거나 roll이 45°를 넘으면 무너진다. roll은 학습 분포 밖 문제이고(이미지만 세워도 −73%), 넓은 시점 차는 정보 부족 문제다(LoFTR도 같은 구간에서 무너짐).
- 실제 키프레임을 앵커로 쓰면 앵커의 포즈 오차가 그대로 전파된다(첫 자리 정렬 online 5.5° vs oracle 2.8°).
- 넓은 기선의 일관성은 쌍 매칭이 아니라 물체 모델에서 나온다. BSDF 최종 포즈는 90° 이상 떨어진 쌍도 5~6° 안에서 일관된다.

**이번 아이디어: 앵커를 실제 키프레임 대신 모델 렌더로 바꾼다**
- 새 프레임의 초기 포즈(트래커 C1)에서 물체 모델을 렌더하고, 그 주변 몇 장을 더 렌더한다.
- [렌더 여러 장 + 실제 프레임 1장]을 VGGT에 넣고, 렌더들의 알려진 포즈로 실제 프레임 포즈를 읽는다.
- 기대하는 효과:
  - 렌더는 초기 포즈 근처라 항상 가까운 뷰다.
  - roll도 초기 포즈 기준으로 맞춰진다.
  - 앵커 포즈는 만든 그대로라 오차가 0이다.
  - 매 프레임이 같은 모델에 직접 맞춰지니 누적이 없다.
  - 미관측 면도 SAM3D prior가 채워주니 가까운 렌더를 만들 수 있다.

**이번 probe의 질문**
- **Q1.** 모델 렌더 앵커로 VGGT가 초기 포즈(C1)의 오차를 줄이는가? 특히 큰 오차(10° 이상) 키프레임에서? 작은 오차 키프레임을 망치지는 않는가?
- **Q2.** 모델에 따라 얼마나 달라지는가? GT 메시(도메인 차이 최소), SAM3D prior(온라인 가능), 관측 GS 지도를 비교한다.
- **Q3.** 수렴 범위(초기 오차 몇 도까지 복구하는가)가 모델 기반 ICP, LoFTR(렌더 vs 실제)보다 넓은가? VGGT → ICP 조합이 각각보다 나은가?
- **Q4 (곁가지).** VGGT depth로 센서 depth 구멍(마스크 안 무효 픽셀)을 메울 수 있는가?

**하지 않는 것**: 본 코드 통합, 지도 학습·메시 채점, 모델 fine-tuning.

---

## 1. 규칙

Probe 1b 지시서 §1과 같다. 차이만 적는다.

**허용**
- `experiments/vggt_probe/p2_*.py` 새 파일.
- `logs/exp_vggt_probe2/`, `outputs/exp_vggt_probe2/...` 새 디렉터리.
- 결과 문서 `EXP_VGGT_PROBE2_RESULTS.md` 1개.

**금지**
- 본 코드와 기존 probe 파일(`probe_lib.py`, `run_probe.py`, `models.py`, `p1b_*.py`) 수정. import해서 재사용한다.
- Probe 1·1b 산출물 수정.
- 9/22 실행 산출물 수정. 읽기 전용이다.

**운영**
- `logs/exp_vggt_probe2/PROGRESS.md`를 단계마다 갱신한다.
- 매니페스트를 남기고, 실패하면 1회 재시도한다.
- 커밋은 사용자가 승인하면 한다.
- 모든 하이퍼파라미터는 A단계 시작 전에 고정한다. 시퀀스별 튜닝은 금지다.

**온라인 가능성 원칙**
- A단계 입력 구성(초기 포즈, 렌더 포즈, 모델)에는 GT를 쓰지 않는다. 초기 포즈는 C1이다.
- GT는 평가와 B단계 교란 생성, 0단계 단위 확인에만 쓴다.

---

## 2. 모델 (렌더 대상)

모든 모델은 BSDF 실행의 물체 좌표계(Probe 1과 같은 게이지)에 놓고 렌더한다. 렌더 결과는 RGB, metric depth, 실루엣 마스크(alpha > 0.5)다.

- **MD-gt: GT 텍스처 메시** (상한, 도메인 차이 최소)
  - 경로: `eval_mesh_cd.load_gt_mesh`와 같은 곳(`datasets/HO3D_v3/models/<obj>/textured_simple.obj`).
  - 꼭짓점을 `GroundTruth.align`으로 run 물체 좌표계로 옮긴다. Probe 1b A3에서 메시 PCA를 옮긴 방식과 같다.
  - 렌더러: 저장소의 `offscreen_renderer.py`(pyrender). 조명·배경 설정은 파일을 따른다.
- **MD-prior: SAM3D prior** (온라인 가능)
  - 9/22 Q2 하네스(`exp_hybrid_realistic_replay.py`)가 초기화 때 prior를 불러와 정합하는 경로를 그대로 쓴다. 파일은 `SAM3D_ROOT/.../<seq>_splat_depth.ply` 등이다.
  - 첫 키프레임 append 전의 정합된 prior를 만든다.
  - 파이프라인이 prior를 렌더하는 표현(Gaussian, `GaussianRunner.render`)으로 렌더한다.
  - 정합된 prior를 체크포인트로 저장하고, 매니페스트에 경로와 정합 Sim(3)을 기록한다.
- **MD-map: 관측 GS 지도**
  - 9/22 Q2 reanchor 재생의 `gs_online/checkpoint_final.pt`. 경로는 9/22 매니페스트에서 찾는다.
  - BSDF 포즈로 만든 최종 지도라 미래 정보가 섞여 있다. "관측 지도 상한"으로 해석하고, 결과 문서에 그렇게 적는다.

---

## 3. 추정기

모든 추정기의 입력은 같다.
- 실제 프레임: RGB, 센서 depth, SAM2 마스크, K
- 초기 포즈 `P0` (c2w, 물체 좌표계)
- 모델

출력은 추정 포즈 c2w다.

**E-V2: VGGT, 렌더 1장 + 실제**
- 배치는 [render@P0, real].
- I1 가상 카메라 warp는 두 장에 공통 f_v를 쓴다(Probe 1 `virtual_focal`). 렌더의 마스크 무게중심은 렌더 실루엣에서 구한다. 배경은 흰색이다.
- 포즈 복원은 Probe 1과 같다(`c2w_v · blockdiag(R,1)`).
- 스케일 s는 두 장의 유효 픽셀 광선거리 비 중앙값으로 구한다. 렌더는 렌더 depth, 실제는 센서 depth를 쓴다.
- 정렬은 render@P0 한 장이다(Probe 1b 정렬 (b)).

**E-V5: VGGT, 렌더 5장 + 실제**
- 배치는 [render@P0, render@P0⊕(±δ, x), render@P0⊕(±δ, y), real]이다.
- `P0⊕(θ, 축)`은 카메라를 모델 중심(렌더 모델 꼭짓점 또는 Gaussian 중심의 평균) 주위로 P0 카메라의 x축 또는 y축을 따라 θ만큼 공전시킨 포즈다. roll은 바뀌지 않는다.
- δ = 10°.
- 정렬은 (b) render@P0 한 장과 (d) 5장 강건 정렬(Probe 1b `align_variant("d")`) 둘 다 보고한다.

**E-V5×2: 반복**
- E-V5 (d)의 결과 포즈 P1에서 다시 렌더해 한 번 더 돌린다.

**E-L: LoFTR (렌더 vs 실제)**
- Probe 1b `p1b_B6.process_pair`와 RANSAC을 그대로 쓴다.
- A = 실제(센서 depth), B = render@P0(렌더 depth, 포즈 P0).
- 3D-3D RANSAC + Procrustes로 상대 포즈를 구하고, 실제 포즈 = P0 ∘ 상대로 만든다.
- 인라이어가 `min_match_with_ref` 미만이면 실패로 두고 P0를 그대로 낸다. 실패 여부는 기록한다.

**E-I: 모델 기반 ICP**
- Open3D point-to-plane ICP를 쓴다. source는 실제 프레임의 마스크 안 센서 depth 역투영, target은 render@P0 depth의 역투영(물체 좌표계, 법선 추정)이다.
- 초기값은 P0. 대응 거리는 2 cm → 1 cm → 5 mm 3단계로, 단계당 최대 30회 반복한다.
- fitness와 inlier RMSE를 기록한다.

**E-V→I: VGGT 결과를 초기값으로 ICP**
- E-V5×2의 결과를 E-I의 초기값으로 쓴다.

**E-FP (선택): FoundationPose 정제기**
- FoundationPose가 설치돼 있고 사용자가 가중치 경로를 알려주면 C단계로 추가한다.
- 모델은 MD-gt 메시와 MD-prior 메시(npz를 trimesh로 변환)를 쓴다.
- 초기 ob_in_cam = inv(P0), 정제 반복 수는 FoundationPose 기본값이다.
- 없으면 건너뛰고 결과 문서에 적는다.

---

## 4. 단계

**0단계. 단위 확인 (필수, AP12·SM1, 통과 전 결과 금지)**
- **0-a. 렌더 정합성:** MD-gt를 GT 포즈에서 렌더한다.
  - 마스크 안에서 렌더 depth와 센서 depth의 차이 중앙값을 mm로 기록한다.
  - 렌더 실루엣과 SAM2 마스크의 IoU 중앙값을 기록한다(손 가림 때문에 1보다 작다).
  - 기준: depth 차이 중앙값 < 10 mm, IoU 중앙값 > 0.6. 못 넘으면 게이지·내부파라미터 변환을 의심하고 중단해 원인을 적는다.
- **0-b.** MD-prior와 MD-map을 GT 포즈에서 렌더한 depth 차이와 IoU를 기록한다(참고값).
- **0-c. 합성 복구:** 실제 프레임 대신 render@GT를 넣는다(도메인 차이 0).
  - 초기값 = GT를 모델 중심 주위 무작위 축으로 10° 돌린 포즈. KF 20개로 한다.
  - E-V5×2, E-L, E-I가 GT를 복구하는지 본다. 기준은 VGGT 중앙값 < 2°.
  - 실패하면 코드 문제로 보고 중단한다.

**A단계. 현실 조건 (주 실험)**
- 초기값: C1(BSDF 소비 시점 포즈).
- 대상: HO3D 13 전 키프레임(GT 있는 것).
- 모델: MD-gt, MD-prior, MD-map.
- 추정기: E-V2, E-V5 (b)/(d), E-V5×2, E-L, E-I, E-V→I.
- 시간이 부족하면 키프레임을 한 개 걸러(짝수 인덱스만) 쓴다. 그렇게 했다고 적는다.

**B단계. 수렴 범위 (통제 교란)**
- 대상: AP12, MPM12, SM1, 시퀀스당 키프레임 40개(고정 시드 무작위).
- 초기값: GT를 모델 중심 주위 무작위 축으로 δ만큼 회전하고, 이동에 N(0, 1 cm) 잡음을 더한다.
- δ ∈ {5, 10, 20, 30, 45, 60}°.
- 모델: MD-gt, MD-prior. 추정기: A단계와 같다.
- E-V5는 렌더 공전각 δ_render ∈ {10°, 20°} 두 가지를 모두 돌린다.

**C단계 (선택).** E-FP를 A단계(MD-gt, MD-prior)에 추가한다.

**D단계 (곁가지, Q4). VGGT depth로 구멍 메우기**
- 대상: HO3D AP12, MPM10, MPM12, SB11, SM1과 YCB mustard0, tomato_soup_can_yalehand0.
- Probe 1 체인 배치 구성(M-V/I1)으로 VGGT를 다시 돌려 depth와 conf를 저장한다.
- GT depth: MD-gt를 GT 포즈에서 원본 카메라로 렌더한 뒤, 같은 I1 매핑(nearest)으로 warp한다.
- 마스크 안 픽셀을 둘로 나눈다: 센서 유효, 센서 무효(구멍). 손 가림은 마스크 밖이라 제외된다.
- VGGT depth 보정 방법 두 가지:
  - (i) 프레임별 스케일 s_i
  - (ii) 프레임별 affine(scale + shift). 둘 다 센서 유효 픽셀로 맞춘다.
- 보고: 시퀀스별로 다음을 적는다. conf 상위 50% 픽셀만 쓴 경우도 병기한다.
  - 구멍 비율
  - 센서 depth의 GT 대비 오차(유효 픽셀)
  - VGGT depth의 GT 대비 오차(유효 픽셀, 구멍 픽셀)
  - 오차는 중앙값과 p90, 단위 mm

---

## 5. 지표

**키프레임마다 (A, B)**
- 초기 오차와 추정 후 오차: GT 대비 회전(°), 이동(mm).

**요약 통계**
- 회전·이동 중앙값과 p90.
- 성공률: 회전 < 5°이면서 이동 < 20 mm. 엄격 기준 < 2°·< 10 mm도 병기한다.
- 개선률: 추정 오차 < 초기 오차인 비율.
- 악화율: 추정 오차 > 초기 오차 + 2°인 비율.

**초기 오차 구간별**
- A단계는 C1 회전 오차 [0,3), [3,10), [10,30), [30,180] 구간으로 나눠 보고한다.
- 구간별 표본 수를 반드시 적는다.

**B단계**
- δ별 성공률 곡선을 추정기 × 모델마다 그린다.

**신뢰 신호** (E-V5 d의 온라인 계산 가능 값)
- 렌더 앵커 재추정 잔차(Probe 1의 T1과 같은 정의)
- 렌더 앵커 사이의 s 퍼짐
- 실제 프레임 마스크 면적 대비 렌더 실루엣 IoU(P0 기준)
- E-I fitness
- 각 신호와 추정 후 회전 오차의 Spearman ρ를 구한다.
- 신호 하위 20%를 버렸을 때(버린 프레임은 P0 유지) 악화율이 어떻게 변하는지도 적는다.

**비용**
- 키프레임당 시간(렌더, VGGT, LoFTR, ICP 각각).

---

## 6. 순서·예산

1. 모델 준비(MD-prior 정합 체크포인트, MD-map 경로 확인) → 0단계.
2. A단계. 키프레임당 약 2초 × 모델 3개 × HO3D 약 2,600 KF ≈ 4~5 GPU·h(2장 병렬 2~3시간). 초과하면 한 개 걸러 쓴다.
3. B단계. 약 1,440건 × 2 δ_render, 약 1~1.5시간.
4. D단계. 약 30~45분.
5. C단계(선택), 결과 문서.

생략 순서(시간 부족 시): C → D → B의 δ_render 20° → A의 MD-map.
반드시 끝낼 것: 0단계, A단계(MD-gt, MD-prior), B단계(δ_render 10°).

---

## 7. 산출물

- `outputs/exp_vggt_probe2/{A,B,D}/<model>/<seq>/`
  - `kf.csv`: 키프레임 × 추정기 행. 열은 초기·추정 c2w, 오차, 성공, 신호, 시간.
  - `manifest.json`
- `outputs/exp_vggt_probe2/models/`: MD-prior 정합 체크포인트, 렌더 설정.
- `logs/exp_vggt_probe2/`: PROGRESS.md, manifests, 표 md.

**결과 문서 `EXP_VGGT_PROBE2_RESULTS.md`**
1. 한 페이지 요약: Q1–Q4의 답과 수치.
2. 0단계 결과.
3. 표 A1: 모델 × 추정기, HO3D 13 평균(회전·이동 중앙값, 성공률, 개선률, 악화율).
4. 표 A2: 초기 오차 구간별(모델 × 추정기).
5. 표 A3: 시퀀스별(MD-prior 기준 주요 추정기).
6. 표 B: δ별 성공률 곡선(수치 표).
7. 표 S: 신뢰 신호.
8. 표 D: depth 구멍.
9. (선택) 표 C: FoundationPose 비교.
10. 계획 이탈, 승인 필요 사항.

**판독 기준 (잠정; 판정은 사용자가 한다, 표는 정확히)**
- **"큰 오차 교정기로 유효"**: MD-prior에서, C1 오차 10° 이상 구간의 추정 후 회전 중앙값이 C1의 절반 이하이다. 동시에 C1 오차 3° 미만 구간의 악화율이 10% 이하이다(신호로 거른 뒤 수치도 병기).
- **"VGGT가 수렴 범위를 넓힌다"**: B단계 δ = 30°에서 E-V5×2 또는 E-V→I의 성공률이 E-I와 E-L보다 20%p 이상 높다.
- **"조합이 낫다"**: E-V→I가 A단계 전 구간에서 E-I와 E-V5×2 중 나은 쪽과 같거나 낫다.
- **"도메인 차이가 병목"**: 같은 추정기에서 MD-prior와 MD-gt의 성공률 차이가 20%p 이상이다.
- **"구멍 메우기 가능"**: 구멍 비율이 5% 이상인 시퀀스에서, VGGT(affine, conf 상위 50%) 구멍 오차 중앙값이 센서 유효 픽셀 오차 중앙값의 2배 이하이다.

---

## 8. 주의

- 렌더와 실제 프레임은 같은 I1 warp 코드로 같은 가상 카메라에 넣는다. 렌더는 원본 K·해상도로 렌더한 뒤 warp한다. 렌더 마스크 무게중심을 쓰므로 렌더와 실제의 중심 회전은 서로 다르다. 포즈 복원 때 각자의 R을 쓴다.
- 실제 프레임은 손 가림 때문에 실루엣이 잘려 있고, 렌더는 온전하다. 이 차이는 이번엔 보정하지 않는다. 손 가림 비율(렌더 실루엣 대비 실제 마스크 면적)을 행마다 기록해, 결과 문서에서 가림 비율별로 나눠 본다.
- MD-map은 미래 정보가 섞인 상한이다. 온라인 결론은 MD-prior 기준으로 쓴다.
- 대칭 물체의 뒤집힘이 섞일 수 있으니 평균 대신 중앙값, p90, 성공률로 보고한다.
- E-L과 E-I는 VGGT와 같은 초기값·같은 렌더를 쓴다. 비교가 공정하도록 추정기별 설정을 A단계 시작 전에 고정한다.
